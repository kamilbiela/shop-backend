config:
    database:
        driver: pdo_mysql
        user:   root
        password: ~
        dbname: shop_test
        host: 127.0.0.1

    timezone: 'UTC'
    disable_exception_handler: true
debug: true

